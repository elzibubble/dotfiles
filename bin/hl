#!/bin/bash
set -euo pipefail

IC=
if [[ "$1" == "-i" ]]; then
  IC="-i"
  shift
fi

PAT="$1"
shift

if [[ "$*" == "" ]]; then
	grep $IC -E --color "$PAT|"
else
	"$@" 2>&1 | grep $IC -E --color "$PAT|"
fi
